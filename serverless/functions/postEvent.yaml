handler: src/main/handler.post_event
events:
  - http:
      path: event/{datasetId}/{version}
      method: post
      private: true
      parameters:
        paths:
          datasetId: true
          version: true
      documentation: ${file(serverless/documentation/postEvent.yaml)}