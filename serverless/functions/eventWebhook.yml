handler: event_collector/handler.event_webhook_legacy
layers:
  - ${ssm:/dataplatform/python-base-lambda-layer/arn}
events:
  - http:
      path: webhook
      method: post
      parameters:
        querystrings:
          token: true

