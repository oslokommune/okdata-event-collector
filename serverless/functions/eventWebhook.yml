handler: src/main/handler.event_webhook
layers:
  - ${ssm:/dataplatform/python-base-lambda-layer/arn}
events:
  - http:
      path: webhook
      method: post
      parameters:
        querystrings:
          token: true
